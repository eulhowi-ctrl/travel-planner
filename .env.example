# Copy to .env for local reference only — this static site has no build step,
# so nothing actually reads .env. The real value lives in the GitHub repo
# secret OPENWEATHER_API_KEY, injected into travel/weather-api.js at deploy
# time by .github/workflows/pages.yml.
OPENWEATHER_API_KEY=your_key_here
