name = "travel-planner-transit-proxy"
main = "transit-proxy.js"
compatibility_date = "2026-01-01"

# Secrets (set via `wrangler secret put <NAME>`, never committed here):
#   ODSAY_API_KEY       - lab.odsay.com 발급 키
#   KAKAO_REST_API_KEY  - Kakao Developers > travel-planner 앱 > 앱 설정 > 앱 키 > REST API 키
#
# Allowed origin for CORS (the deployed site). Update if the domain changes.
[vars]
ALLOWED_ORIGIN = "https://eulhowi-ctrl.github.io"
